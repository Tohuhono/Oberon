name: Smoke Tests

on:
  workflow_call:
    inputs:
      repository:
        required: true
        type: string
      ref:
        required: true
        type: string
      project:
        required: false
        default: ""
        type: string
      base_url:
        required: false
        type: string
    secrets:
      VERCEL_PROTECTION_BYPASS:
        required: false
      VERCEL_API_TOKEN:
        required: false

jobs:
  smoke:
    name: Smoke Test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
        with:
          repository: ${{ inputs.repository }}
          ref: ${{ inputs.ref }}
      - name: Use Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24.x"
          package-manager-cache: false
      - name: Corepack
        run: corepack enable pnpm
      - name: Enable pnpm cache
        uses: actions/setup-node@v6
        with:
          cache: pnpm
      - name: Cache turbo build setup
        uses: actions/cache@v5
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-turbo-
      - name: Install dependencies
        run: pnpm i
      - name: Install Playwright browsers
        run: pnpx playwright install --with-deps chromium
      - name: Run Smoke (deployed)
        run: pnpm test:smoke -- --project "${{ inputs.project }}"
        env:
          PLAYWRIGHT_BASE_URL: ${{ inputs.base_url }}
          VERCEL_PROTECTION_BYPASS: ${{ secrets.VERCEL_PROTECTION_BYPASS }}
      - name: Publish Playwright smoke report
        if: ${{ failure() }}
        uses: ./.github/actions/deploy-playwright-report
        with:
          artifact_name: playwright-smoke-report-${{ inputs.project != '' && inputs.project || 'default' }}
          artifact_path: dev/playwright/.playwright/report
          report_dir: dev/playwright/.playwright/report
          summary_label: View report for ${{ inputs.project }}
          vercel_token: ${{ secrets.VERCEL_API_TOKEN }}
