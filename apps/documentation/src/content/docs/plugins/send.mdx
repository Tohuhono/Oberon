# Send Adapter

The send adapter is used to send verification requests to users, usually by
email.

```tsx
export type OberonSendAdapter = {
  sendVerificationRequest: (props: {
    email: string
    token: string
    url: string
  }) => Promise<void>
}
```
