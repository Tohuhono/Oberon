---
"@oberoncms/core": patch
"create-oberon-app": patch
---

Respect gitignore during shared Playwright test discovery, simplify shared auth
redirect handling, and align the create-oberon-app test container with the
shared Playwright defaults used across the repo.
