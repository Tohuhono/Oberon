---
"@tohuhono/tailwind-config": patch
---

Migrate the shared Tailwind setup to the v4 CSS-first model:

- replace the legacy JS config export with CSS exports
- consolidate shared plugins, tokens, utilities, animations, and theme variables
  into the main CSS export
